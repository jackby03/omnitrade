# ENG-006: Engine Crate Wiring

| Field | Value |
|---|---|
| Status | `TODO` |
| Phase | 3 |
| Crate | `omnitrade-engine` |
| Target | `src/lib.rs` |
| Depends On | `ENG-001`, `ENG-002`, `ENG-003`, `ENG-004`, `ENG-005` |
| Blocks | — |
| Complexity | S |

## Description

Wire all engine modules together with module declarations and public re-exports in `lib.rs`.

## Requirements

- Module declarations for: `error`, `fill`, `orderbook`, `fees`, `matching`, `account`
- Public re-exports:
  - `pub use error::EngineError`
  - `pub use fill::OrderFill`
  - `pub use orderbook::L2OrderBook`
  - `pub use fees::{FeeSchedule, SlippageModel}`
  - `pub use matching::MatchingEngine`
  - `pub use account::AccountState`

## Acceptance Criteria

- [ ] `cargo check -p omnitrade-engine` compiles
- [ ] All re-exported types are accessible as `omnitrade_engine::TypeName`
- [ ] Doc comment on `lib.rs` describing the crate's purpose

## Cargo Dependencies

Update `Cargo.toml`:
```toml
[dependencies]
omnitrade-core = { path = "../omnitrade-core" }
thiserror = "2"
```
